extern crate rocket;
extern crate rocket_contrib;

use dash_rs::model::level::ListedLevel;


use crate::gd;

// #[get("/search/<string>")]
// pub fn search(string: &str) -> rocket_contrib::json::Json<Vec<ListedLevel>>
// {
//     let raw_data = gd::prepare_search_request(string);
//
// }
